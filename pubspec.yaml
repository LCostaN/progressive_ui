name: progressive_ui
description: Priority-based responsive layout utilities for Flutter.
version: 0.1.0
homepage: https://github.com/LCostaN/progressive_ui
repository: https://github.com/LCostaN/progressive_ui
issue_tracker: https://github.com/LCostaN/progressive_ui/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
# flutter:
